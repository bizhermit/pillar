version: '3'
services:
  node:
    container_name: 'pillar'
    build:
      context: '../'
      dockerfile: '.prodcontainer/Dockerfile'
    image: 'pillar_image'
    tty: true
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    ports:
      - '${PORT:-3000}:3000'
